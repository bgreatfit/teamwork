sudo: required
language: generic
services:
- docker
before_install:
- docker-compose  up -d
script:
- echo $DJANGO_SECRET_KEY
- echo "DONE"
#docker-compose  exec web python manage.py test
deploy:
  provider: script
  script: bash docker_push.sh
  skip_cleanup: true
  on:
    branch: master
